<h1>Show Team</h1>

<ul>

  <li>
    <strong>Name:</strong>
    <%= @team.name %>
  </li>

</ul>

<span><%= link "Edit Team #{@team.name}", to: Routes.team_path(@conn, :edit, @current_company, @team) %></span>
<span><%= link "Delete Team #{@team.name}", to: Routes.team_path(@conn, :delete, @current_company, @team), method: :delete, data: [confirm: "Are you sure?"] %></span>

<ul>
  <%= for retro <- @team.retros do %>
    <span><%= link "Go to retro: #{retro.name}", to: Routes.answer_index_path(@conn, :index, @current_company,  retro) %> </span><br>
  <% end %>
</ul>

<span><%= link "New retro", to: Routes.retro_path(@conn, :new, @current_company, team_id: @team.id) %> </span>
